# HANDOFF — RM-Cortex

- 更新日期：2026-07-26
- 当前阶段：Phase 1 的 M1–M4 已实现；全局＋八角色行为树/A* 基线与 M5 数据 pilot 已接通
- 项目目标：建立可验证的 RMUC 裁判、批量仿真、多智能体训练与后续 sim2real 链路

## 本地开发资料

维护者的规则审计、Digest、架构说明与实施计划保存在本地 `docs/superpowers/`，由 `.gitignore` 排除，不作为公开仓库内容。规则真值层级为：**最新官方规则/答疑 > V1.5.0 PDF > 本地 Digest > 设计/实现**。官方手册和数据集同样只在本地保存。

## 已冻结的设计

- `rm_referee` 是唯一裁判真值层，只依赖 Torch，不导入 Isaac。
- Phase 1 包含纯 Torch 运动学后端和 Isaac Lab `DirectMARLEnv` adapter；两端共享状态、规则、事件和随机样本。
- 裁判固定 10Hz，运动学默认 60Hz，策略默认 5Hz。
- 高层普通射击目标为 9 类：5 个敌方地面机器人、敌方基地、敌方前哨站、己方能量机关、无目标。
- Phase 1 MAPPO 默认使用全 16 槽队伍共享信念状态；`observation_mode="oracle"` 仅保留为全信息性能上界，训练与评估必须使用同一种模式。
- 低层地面导航固定为纯 Torch 世界中的 0.05m 网格 A*，不作为 PPO/BC 学习目标；高层 baseline 每个 5Hz 策略步由一棵全局树和八棵角色树选择团队意图、任务与叶节点。
- 常规命中率、小陀螺收益、雷达漂移和工程取件抽象属于 `[SIM]`，必须可配置、可标定。
- 真实数据用于统计校准，以及高层团队任务、角色任务、可靠目标和 BT 叶节点/option 选择；不把 1Hz、缺目标归因的日志当作逐发精确真值或 0.2 秒控制动作。

## 不得重新引入的错误

| 错误说法 | 正确语义 |
|---|---|
| 热量/功率超限扣血 | 热量锁定；功率缓冲耗尽后底盘断电 5 秒，均不扣血 |
| 50/200ms 是武器射速上限 | 是单块装甲接受 17/42mm 伤害的检测间隔 |
| 弹量是队伍共享 | 弹量按机器人；只有金币购买累计按队 |
| 42mm 购买上限 1000 | 42mm 为 100 发/队，17mm 才是 1000 发/队 |
| 读条复活为满血 | 10% HP、虚弱、最长 30 秒无敌 |
| 基地护甲展开自带减伤 | 只改变可攻击几何 |
| 空中是普通受击单位 | 不受普通伤害/撞击，不回血、不复活 |
| 雷达直接固定增加 `P` | 先更新连续量 `x`，再执行 `P += x` |
| 堡垒高 50mm | 图 4-25 标注高度为 150mm |
| 红蓝场地模块只沿 `x` 镜像 | 战场为中心对称；成对模块与出生点按 180° 旋转 |

## 已冻结与仍需保留的歧义

- 热量正文使用 `Q1>Q2`，流程图使用 `Q1≥Q2`；项目已冻结为 `Q1≥Q2`，不保留兼容开关。
- 官方图未定义场地坐标原点/轴向；少数局部模块没有单独标注全局中心或封闭轮廓。
- 手册没有给出常规命中、小陀螺收益、雷达漂移或不完整飞镖识别的概率函数。

## 当前实现

`rm-sim/` 已形成完整的纯 Torch 闭环：

- M1：16 槽批量裁判状态，覆盖战斗、热量/功率、经济、升级/科技、复活、角色机制、区域增益、能量机关、飞镖、雷达与终局。
- M2：28×15m 解析场地、50mm 地形高度图、手册 1°–2° 横坡的 1.5° 中值 `[SIM]`、支持逐顶点高度的多边形表面、150mm 堡垒及六个 20° 坡面、中央高地 200–350mm/10.5° 连接坡、梯形高地 200–400mm/23°/43° 分层、17° 飞坡、70mm/240mm 起伏路、图 4-5 基地/前哨站/堡垒中心、可确认的物理设施平面边界与独立 `[SIM]` 裁判区域、带安全间距的八轮迭代接触约束、区域、LOS、装甲交点、可配置命中模型、信念/Oracle 双实体观测，以及目标优先/分角色战术两类脚本对手。
- M3：可选 `DirectMARLEnv` 场景、共用地形 primitive（非矩形及逐顶点高度 footprint 以确定性耳切法生成网格，网格顶点逐点叠加场地横坡，起伏路生成物理条带）、Torch 运动学驱动的可视标记同步、统一 `RuleInputs` 归一化与无 Isaac 导入安全边界。
- M4：参数共享 actor、角色/队伍 embedding、51 维全实体共享编码器与注意力、集中式 critic、异构动作头与 mask、GAE、clipped MAPPO、checkpoint 和脚本对手评估。信念实体由 32 维基础状态、位置/决策状态两类时延、10 维有界 PSD 协方差上三角与 7 类来源组成；rollout 保留 `[时间, 环境, 观察者, 目标, 特征]` 轴，PPO minibatch 不会混淆观察者与目标。
- 高层 baseline：一棵全局树先发布团队意图，八棵独立角色树再为英雄、工程、步兵 3/4、空中、哨兵、基地和前哨站选择任务/叶节点。只有五个地面移动角色使用带缓存的 0.05m、8 邻域 A*；它只规划静态边界与 footprint-aware 障碍，坡面不附加代价，动态单位由既有局部分离处理，空中和建筑不走 A*。战斗、经济、雷达和兵种规则动作复用确定性 full-action 执行器。
- M5 pilot：只读 RMUC SQLite 适配器按队伍连通分量和时间严格切分数据，保留缺测/异常/重复质量掩码；离线 featurizer 生成与 MAPPO 同构的 34D actor 和 16×51D belief entity 输入。BC 只监督未来 5 秒截断位移与区间射击事件代理。迁移保留 MAPPO 动作头与 critic MLP 参数的初始化，但共享角色/队伍 embedding 也进入 value 路径，因此不能声称 critic 函数完全未变；保留基地/前哨站角色行也不能隔离共享骨干变化。

行为树评估使用：

```bash
cd rm-sim
python scripts/evaluate_behavior_tree.py \
  --opponent tactical \
  --seeds 1007 \
  --device cpu
```

每个 seed 分别让 BT 执红、执蓝，形成两条独立换边 leg；每条运行到提前终局或 420 秒上限。`--max-policy-steps` 只用于冒烟截断，未完成 leg 单独计数且不进入 `balanced_score`。双方都读取完整世界状态并可输出完整 `WorldActions`，所以结果只属于 oracle/full-action 工程对照，不与 belief-MAPPO 胜率直接横比。seeds `1007/2007/3007` 的实现级 pilot 已完成六条 420 秒 leg：A* `227/227` 成功、BT 对 tactical 为 `3–3`、`balanced_score=0.5`，平均累计队伍回报 `6.539` 对 `10.851`。这不是正式胜率结论。

雷达是独立于场内前哨站的队伍级系统。规则层已覆盖 `x→P` 连续标记、己方/敌方真值显示阈值、地面机器人 15%/20% 易伤、双倍易伤、干扰压制和空中机器人激光反制。MAPPO 暂借前哨站策略槽承载队伍级雷达动作头，这只是 16 槽参数共享布局中的控制代理，不改变裁判层语义。

完整 420 秒 MP4 可视化使用确定性的战术脚本而非训练 checkpoint：英雄部署、工程支援/重建、步兵分路、哨兵防守、官方远程购弹、目标集火与虚弱撤回均有独立任务。空中仅在脚本呼叫且免费支援库存可用时分波次出动，停机坪接触、暂停支援和雷达锁定均会阻止开火；运动范围受手册 4.5 节飞行区及 14m 卡环/2.4m 安全绳的 `[SIM]` 包络约束，规则层仍完整支持免费时长耗尽后的 1 金币/秒付费支援。

`ruff`、`mypy`、`compileall`、完整 pytest 与命令行训练/评估冒烟均通过。实体回归覆盖隐藏真值隔离、恒速预测、协方差半正定与有界增长、本地重捕获、雷达估计/确认、重复观测幂等、rollout 轴保持、PPO log-prob 一致和实体编码器真实梯度更新；雷达回归精确覆盖己方 `P=50`、敌方 `P=100` 的真值显示边界。接触与地形回归覆盖同点分离、随机批量运动、静态障碍、坡面逐顶点插值、堡垒分面和完整脚本对局。seed 1007 的 BT 红方对 tactical 蓝方完整 420 秒 H.264 导出为 1120×706、421 帧、21.05 秒，逐策略步外廓检查的最小地面单位净间距为 0.0255m，蓝方获胜。场地使用严格等比例 36px/m 变换，28×15m 对应 1008×540px；画面按相对局部场地面的高度分层设色，带 50mm 等高色带、坡向箭头和独立高度图例，阵营色只用于边界。此前的吞吐数字基于四轮接触求解，改为八轮后应在 GPU 空闲时重新基准，不沿用旧数字。

## 尚未完成的验证

- BT、A* 与 paired-side evaluator 已通过 contract、CLI 冒烟及三 seed/六 leg 的完整 420 秒换边 pilot；结果对 seed 与阵营仍敏感，后续需要扩大预注册 seed 集合并增加到达率、用时、实际/规划路程和碰撞修正诊断，不能把当前 `3–3` 当作正式胜率。
- API 执行会话没有 `DISPLAY`，因此交互式 Isaac 视口尚未人工检查；`scripts/visualize_isaac.py` 已用同一战术控制器通过原生 Isaac Lab CPU 无头冒烟，并可在带图形桌面的 launcher 中运行。
- Isaac `DirectMARLEnv` 仍保留每个 agent 34 维的公共观测契约；它已共用同一个 `ObservationBuilder`，但同一套实体注意力 MAPPO 若要直接在 Isaac 中训练，仍需增加显式 observation adapter，不能静默改变现有 PettingZoo space。
- 此前的原生 Isaac Lab CPU 无头场景冒烟已通过；本轮调整了 Isaac 雷达绝对坐标与单脉冲解码，但当前主机解释器未安装 `isaaclab`，合入后需在 Isaac 运行环境重新执行 `scripts/isaac_smoke.py --headless`。
- 官方未提供可直接使用的 CAD/USD；基地、前哨站和堡垒中心已由图 4-5 尺寸换算为项目坐标，梯形高地、公路区、堡垒、飞坡和起伏路采用详图外廓。补给区/停机坪全局中心、中央高地内部层面、装配区/公路内部坡面、隧道中心和飞行区闭合边界没有唯一完整定位，继续标记为 `[SIM]` / `[AMB]`，后续应以官方资产或实测场地标定替换。
- MAPPO 已完成 50-update belief pilot，在两个 64 局种子上的红方单侧评估均为 62–2；它仍不是默认 200-update、多训练种子、换边配对统计实验，因此不能当作正式胜率曲线，也不能与 oracle/full-action BT 结果直接比较。
- 当前 belief tracker 使用可配置的 `[SIM]` 恒速过程噪声与传感器噪声；后续应从授权数据标定这些参数，并评估更强的运动模型。集中式 critic 仍使用全局真值，这是 CTDE 训练设计，不是 actor 感知输入。
- checkpoint 已记录策略 schema、观测模式与完整构造参数；早于 belief 实体编码器的本地 checkpoint 与 schema v3 不兼容，需要重训或显式迁移。
- 离线 BC 首版只保留 10 秒因果历史；更早离开视野的目标会回退到有界先验，与线上从开局持续维护的 belief tracker 不完全一致。区间开火标签也会混入未来死亡/复活等结果，不能解释为瞬时许可。
- RMUC BC pilot 使用 8,192/2,048 个南部/东部窗口。验证集导航 `MAE=1.458m, RMSE=2.219m`；原地基线为 `1.368m/2.427m`，说明它降低了少量大误差，却在大量静止/小位移样本上引入误差。区间射击代理为 `F1=0.578`，不包含目标选择监督。
- 当前骨干迁移未通过下游采用门槛：相同 seed 7、64 环境、50 updates（204,800 环境步）后，seed 1007 的红方单侧确定性评估为 `0–64, red return=-3.672`；纯 MAPPO 基线为 `62–2, +23.546`。这不是正向结果，也不能证明人类数据有害；`--pretrained-actor` 仅作为可复现负向消融入口，不默认启用。
- RMUC 人类数据的下一阶段用途是监督高层团队任务、角色任务、可可靠推导的目标与 BT 叶节点/option；低层保持固定 0.05m A*。不再把五秒位移 BC、仅导航的 BC/PPO 或 goal-only 迁移当作项目方向。
- M5 的命中、雷达漂移和传感器协方差标定仍待实现；数据集没有相机、LOS、雷达坐标或逐发目标归因，相关参数继续标记为 `[SIM]`。

`RMUC-OfflineRL/` 是本地参考仓库，不导入、不修改、不提交。PDF、数据集、Isaac 运行时、训练日志与 checkpoint 均已由顶层 `.gitignore` 排除。
